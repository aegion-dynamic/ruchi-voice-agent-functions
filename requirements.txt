# ---- Core FastAPI runtime ----
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
python-dotenv>=1.0,<2
httpx>=0.27,<1
python-multipart>=0.0.9,<1

# ---- Server-side voice (free, no API keys) ----
# edge-tts: Microsoft neural TTS, has Telugu voices te-IN-ShrutiNeural /
#           te-IN-MohanNeural. Needs internet.
edge-tts>=6.1,<8
# vosk: fully offline speech recognition. Needs the Telugu model in
#       models/vosk-model-small-te-0.42 (see README).
vosk>=0.3.44,<1

# ---- Tests ----
pytest>=8,<10
pytest-asyncio>=0.23,<2
